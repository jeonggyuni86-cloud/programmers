
// * 동시성과 코루틴

// 0. 코루틴은 코틀린 표준 라이브러리가 아니라 별도 라이브러리다.

// 1. 스레드의 문제
// 자바에서 동시에 여러 일을 하려면 스레드를 만들었다. 그런데 스레드는 비싸다.
// - 하나에 1mb 안팎의 메모리를 쓴다. 수 천개를 쓰면 메모리가 바닥난다.
// - 스레드를 바꿔가며 실행하는 데(컨텍스트 스위칭) 비용이 발생한다.
// - Thread.sleep으로 기다리는 동안 그 스레드는 아무 일도 못 하고 묶여있다.

// 2. 코루틴이란
// 중단했다가 다시 나중에 재개할 수 있는 작업 단위이다. 흔히 '경량 스레드' 리고 부른다.
// 코루틴은 스레드를 '대신'하는 것이 아니라 스레드 '위에서' 돌아간다.
// 서버에 요청을 보내 놓고 응답을 기다리는 상황을 생각해보자.

// [스레드]
// 응답이 올 때 까지 그 일을 맡은 스레드는 아무것도 못 하고 붙잡혀있다.
// 일은 안 하는데 자리만 차지하고 앉아 있는 것이다.
// 스레드가 200개면 동시에 200명까지가 끝이고, 201번째 사람은 앞이 끝나야 시작한다.
// 200개 전부가 '그냥 기다리는 중' 인데도 대기한다.

// [코루틴]
