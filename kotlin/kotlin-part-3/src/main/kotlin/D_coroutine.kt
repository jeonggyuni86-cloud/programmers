
// * 동시성과 코루틴

// 0. 코루틴은 코틀린 표준 라이브러리가 아니라 별도 라이브러리다.

// 1. 스레드의 문제
// 자바에서 동시에 여러 일을 하려면 스레드를 만들었다. 그런데 스레드는 비싸다.
// - 하나에 1mb 안팎의 메모리를 쓴다. 수 천개를 쓰면 메모리가 바닥난다.
// - 스레드를 바꿔가며 실행하는 데(컨텍스트 스위칭) 비용이 발생한다.
// - Thread.sleep으로 기다리는 동안 그 스레드는 아무 일도 못 하고 묶여있다.