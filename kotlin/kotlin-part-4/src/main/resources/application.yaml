spring:
  application:
    name: kotlin-part-4
  datasource:
    url: jdbc:mysql://localhost:3306/kotlin_board?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: info