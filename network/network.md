# AWS 인프라를 위한 네트워크 기초

## 1. IP 주소

### 1-1. 생김새

IPv4 주소는 32비트이다.
8비트씩 4덩어리로 나누고, 점으로 구분한다

```
192.168.10.25
```
- 한 덩어리를 옥텟이라 부른다.
- 옥텟 하나는 0 ~ 255
- 전체 개수는 2^32 = 대략 43억 개

#### 옥텟을 2진수로 보기
10 진수는 172는 아래와 같이 표기할 수 있다.

```
자릿값     100   10   1
            1   7   2  -> 1 * 100 + 7 * 10 + 1 => 172
```

2진수
- 자릿값이 10배씩이 아닌 **2배씩** 커진다.
- 각 칸에는 **0 또는 1만** 들어간다.

```
128 64  32  16  8   4   2   1
 1   0   1   0  1   1   0   0
 
 -> 128 + 32 + 8 + 4 = 172
```
그래서 172 = 10101100이다

#### 자주 나오는 값
```
10  = 00001010
172 = 10101100
192 = 11000000
255 = 11111111
0   = 00000000
```
- 옥텟은 8비가 전부 1이면 255이기 때문에 범위가 0 ~ 255이다.

### 1-2 주소는 두 부분으로 나뉜다
- 네트우크부 / 호스트부
- 네트워크부 : 어느 동네인지
- 호스트부 : 그 동네의 몇 번지인지

```
192.168.10.25
네트워크부 / 호스트부
```

### 1-3 서브넷 마스크 - 경계를 알려주는 숫자

주소만 보면 어디까지가 동네인지 알 수 없다.
그래서 "앞에서 몇 비트까지가 동네(네트워크)다" 라는 정보를 따로 준다.
바로 **서브넷 마스크** 다.

### 마스크는 1과 0으로 경계를 표시한다.

IP는 2진수로 풀면 32비트이다.
마스크도 똑같이 32비트인데, 네트워크부 자리는 1, 호스트부는 0으로 채운다.

```
주소  : 192.168.10.25 = 11000000.10101000.00001010.00011001
마스크 : 255.255.255.0 = 11111111.11111111.11111111.00000000
-> 1로된 부분이 네트워크부이고, 0이 호스트부이다.
```

그래서 앞 24비트(192.168.10)가 이 동네를 뜻하고, 25가 번지를 뜻한다.

#### 같은 주소, 다른 마스크

```
주소  : 192.168.10.25 = 11000000.10101000.00001010.00011001
마스크 : 255.255.255.0 = 11111111.11111111.00000000.00000000
-> 1로된 부분이 네트워크부이고, 0이 호스트부이다.
```

주소는 같은데 마스크가 다르면 동네의 크기가 달라진다.
**IP 주소는 항상 마스크와 세트로 다녀야 의미가 완성된다.**

#### 마스크를 짧계 쓰면 CIDR

CIDR = Classless Inter-Domain Routing. "사이더"라고 읽는다.

- Classless - 네트워크부 길이를 8/16/24로 고정하지 않고 아무 숫자나 쓴다.
- Inter-Domain Routing - 원래는 네트워크 사이의 라우팅 규칙 이름. 거기서 쓰는 /24 표기 자체를 CIDR라고 부른다.

마스크에서 1의 개수를 세서 /뒤에 쓴다
```
255.255.255.0   -> 1이 24 -> /24
255.255.0.0     -> 1이 16 -> /16
255.0.0.0       -> 1이 8 -> /8
```

```
192.168.10.25/24
192.168.10.25 + 255.255.255.0
```
둘은 완전히 같은 의미다. 요즘은 /24쪽으로 주로 사용한다

### 1-4 CIDER로 네트워크 크기 읽기

네트워크부 길이를 / 뒤 숫자로 쓴다.
```
10.0.0.0/16 네트워크부 16비트
10.0.1.0/24 네트워크부 24비트
10.0.1.0/28 네트워크부 28비트
```
주소 개수 계산. 32에서 고정된 비트를 빼면 호스트부 비트가 남는다

```
개수 = 2^(32 - n)
/16 => 호스트부 16비트 => 2^16 = 65,536
/24 => 호스트부 8비트 => 2^8 = 256
/28 => 호스트부 4비트 => 2^4 = 16
```

**뒤 숫자는 고정된 부분의 길이다. 고정이 길수록 남는 자리가 적다. = 네트워크가 적다**

#### 장비에 줄 수 없는 주소 2개

호스트부가 전부 0인 것과 전부 1인 것은 예약돼 있다.
```
10.0.1.0/24 에서

10.0.1.0 호스트부 00000000 네트워크 주소 -> "이 동네 전체"를 가리키는 이름표
10.0.1.255 호스트부 11111111 브로드캐스트 -> "이 동네 전원에게" 보내는 주소
10.0.1.1 ~ 10.0.1.254 실제 줄 수 있는 주소 범위
```

- .0   : 아파트 단지 이름
- .255 : 전 세대 안내방송

둘 다 특정 집이 아니다. 그래서 /24는 256개지만 장비는 254대 까지다.

### 1-5. 사설 IP와 공인 IP

인터넷 = 전 세계 라우터가 서로 연결된 망이다.
그 망에서 통하는 주소가 **공인 IP**고, 인터넷 라우터는 공인 IP의 경로만 알고 있다.

인터넷 라우터에 등록하지 않기로 약속한 대역이 셋 있다.

```
10.0.0.0     ~ 10.255.255.255     10.0.0.0/8
172.16.0.0   ~ 172.31.255.255     172.16.0.0/12
192.168.0.0  ~ 192.168.255.255    192.168.0.0/16
```

- 이 대역은 **사설 IP**이다.
- 인터넷 라우터는 이 주소의 경로를 모른다. 패킷이 오면 버린다. 밖으로 못 나간다.
- 그래서 회사마다, 집마다 같은 주소를 겹쳐 써도 된다.
- 이 대역 밖의 주소가 **공인 IP**디. 전 세계에 하나뿐이고, 어디에서든 도달된다.

우리가 본 것
- 집 공유기 -> 192.168.x.x
- docker-compose 컨테이너 -> 178.18.x.x 같은 주소
- AWS VPC -> 10.x.x.x 를 가장 많이 쓴다.

전부 사설 IP다.

사설 IP 장비가 인터넷에 나가려면 누군가 공인 IP로 바꿔줘야 한다.
그 역할이 **NAT**이다. 집 공유기가 하는 일이고, AWS는 NAT Gateway가 한다.

NAT = Network Address Translation. 네트워크 주소 변환.

```
나갈 때 10.0.11.25 -> NAT -> 3.x.x.x (NAT의 공인 IP)로 바꿔서 내보냄
들어올 때 3.x.x.x -> NAT -> 10.0.11.25로 되돌려서 전달
```

- NAT는 누가 보냈는지" 표를 들고 있다가 응답을 원래 장비에 돌려준다.
- 그래서 사설 IP 장비 여러 대가 공인 IP 하나를 나눠 쓴다.
- 밖에서 먼저 들어오는 건 표에 없어서 버려진다. -> "나가기만 되는 문"

## 2. VPC

### 2-1. 정의

VPC = Virtual Private Cloud

AWS 안에 논리적으로 격리된 **나만의 네트워크**다.
회사 사무실에서 깔린 사내망을 클라우드에 옮긴 것이라고 보면 된다.

### 2-2. 만들 때 정하는 것
CIDR 하나를 정한다. VPC가 차지할 IP 범위다.

```
10.0.0.0/16
```

CIDR은 특정 전용이 아니다. IP 범위를 적어야 하는 곳이면 어디든 쓴다.
```
VPC          10.0.0.0/16       전체 범위
서브넷        10.0.1.0/24       그 안의 조각
라우트 대상   0.0.0.0/0         모든 주소
SG 소스       0.0.0.0/0         어디서든
SG 소스       203.0.113.5/32    이 IP 하나만 (내 IP)
```

- 한번 정하면 바꾸기 어렵다. 넉넉하게 /16으로 잡는다.
- 허용 범위는 /16 ~ /28
- 사설 대역에서 고른다.

### 2-3. 성질

- VPC는 **리전**단위다. 서울 리전에서 만들면 서울 리전 전체에 걸친다.
- VPC 끼리는 기본적으로 완전히 격리된다.
- VPC 안의 자원은 전부 사설 IP를 받는다.
- 그래서 인터넷 연결은 **따로 붙여야**한다. 기본적으론 안 된다.












































